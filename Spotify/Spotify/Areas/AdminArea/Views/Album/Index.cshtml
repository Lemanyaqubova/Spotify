@model List<Album>

<div class="container">
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Album Table</h4>
                    @if (User.IsInRole("Moderator"))
                    {
                        <a asp-controller="Album" asp-action="Create" class="btn btn-success">Create Album</a>
                    }

                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Image</th>
                                    <th>Name</th>
                                    <th>Artist</th>
                                    <th>Genre</th>
                                    <th>Setting</th>

                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            <img src="~/assets/images/@item.ImageUrl" style="border-radius: 2px; width: 100px; height: 100px;">
                                        </td>
                                        <td>@item.Name</td>
                                        <td>@item.Artist.FullName</td>
                                        <td>@item.Category.Name</td>
                                        <td>
                                            <a asp-action="detaill" asp-route-id="@item.Id" class="btn btn-outline-primary">
                                                <i class="fa-solid fa-circle-info"></i>
                                            </a>
                                            <a asp-action="update" asp-route-id="@item.Id" class="btn btn-outline-warning">
                                                <i class="fa-solid fa-pen-to-square"></i>
                                            </a>
                                            @if (User.IsInRole("Moderator"))
                                            {
                                                <a asp-action="delete" asp-route-id="@item.Id" class="btn btn-outline-danger">
                                                    <i class="fa-solid fa-trash"></i>
                                                </a>
                                            }
                                        </td>
                                    </tr>
                                }

                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


